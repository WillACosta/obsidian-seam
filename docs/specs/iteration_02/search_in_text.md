Improve the search engine to support "In-Note search", when typing a query the system should be able to search inside note's content.

The search will functions the same way it works currently, with the addition to also search inside a note file.

The results listing should highlights the found query text in each found notes, it's the same behavior as the built-in Obsidian search. You may look for the Obsidian's Skill definitions and official docs to understand its native search behavior. Specially regarding the highlighting of query in search results.

When a query is found in a note's content, you should display the portion of text in which that query exists. It can be a short and small text, just to improve user's experience and the "Status and Visibility of the system" principle.
