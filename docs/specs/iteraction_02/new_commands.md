Add new commands to the Seam's built-in command palette.

- Move note to the "Permanent" folder (dynamic name, see below)
- Archive note

These commands will have an impact in the current open note, if none is opened they should not be visible in the command palette.

We should analise the possibility of extracting the user defined directory for the "Permanent" folder in the plugin's settings.
