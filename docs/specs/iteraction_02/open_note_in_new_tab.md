Add the ability to open a new in a new tab when:

`cmd (mac) + enter`
`ctrl (win, linux) + enter`
`click + enter`

Currently when we search for a note and use any of the above shortcuts it doesn't open. A natural behavior should be open it in a new tab, otherwise open in the current active tab (default and current behavior).
