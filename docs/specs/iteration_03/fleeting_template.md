## Add support to use a template when creating a new note in "Fleeting", none as default.

Add a new option to the plugin's settings to allow users to set a template that will be used when creating a new note from the Seam's command palette.
