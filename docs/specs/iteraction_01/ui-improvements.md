## Folder name on search listing

Improve the folder name in the search results listing, currently it's just a simple text, we should add a different styling to it, so users can tell the difference between note's name and directory it's stored.

Example:

```
KiCad PCB Design
(folder_icon) Fleeting
```

Note that "(fo∂er_icon)" is only shown when icons are enabled in the plugin options.

## Add options for using Icons

Add an option to enable the use icons in the plugin's UI.

## Improve Commands UI listing

The current commands showed when opening the command palette of the plugin has a left border on the left side, remove it and add a simple "command icon" if icons are enabled, if not leave the text only.
