1. Add Intellisense for local paths in Seam's settings tab.

The idea is to help users when they need to define paths for the folders and fleeting template inside Seam's settings tab.

Obsidian's native settings has support for it. For example when defining a default folder to create new notes in. The text input suggests locations found in the Vault.

2. Update the `Universal Palette` placeholder to: "Search notes, #tags or run Seam commands."

3. We should hide the command display for default, so we can have a clean and minimal UI.

```
Search notes, #tags or run Seam commands.

At the bottom of the Universal Palette, we can display a muted text to help users (centered and small text):

"arrows up/down icons" to navigate "enter icon" to select "esc" to dismiss ">" to show commands
This helper texts UI is the same as used in the Obsidian's native Command Palette.
```