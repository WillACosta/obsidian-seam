## Remove temporary tags and properties if a note is moved to the "Permanent" folder

In both scenarios:

- When adding a tag
- When using the Universal Palette command

Currently if a note is moved from the "Archived" section to the "Permanent" it doesn't have the temporary tags and properties removed, so the predefined "#archived" stuck with the note.
